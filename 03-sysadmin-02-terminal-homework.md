# Домашнее задание к занятию "3.2. Работа в терминале, лекция 2"

1. Какого типа команда `cd`? Попробуйте объяснить, почему она именно такого типа; опишите ход своих мыслей, если считаете что она могла бы быть другого типа.
1. Какая альтернатива без pipe команде `grep <some_string> | wc -l`? `man grep` поможет в ответе на этот вопрос.
1. Какой процесс с PID `1` является родителем для всех процессов в вашей виртуальной машине Ubuntu 20.04? Знакомы ли вы с этой системой и ее альтернативами?
1. Как будет выглядеть команда, которая перенаправит вывод stderr `ls` на другую сессию терминала?
1. Получится ли одновременно передать команде файл на stdin и вывести ее stdout в другой файл? Приведите работающий пример.
1. Получится ли вывести находясь в графическом режиме данные из PTY в какой-либо из эмуляторов TTY? Сможете ли вы наблюдать выводимые данные?
1. Выполните команду `bash 5>&1`. К чему она приведет? Что будет, если вы выполните `echo netology > /proc/$$/fd/5`? Почему так происходит?
1. Получится ли в качестве входного потока для pipe использовать только stderr команды? Используя знание из предудщего вопроса предложите способ как это сделать поменяв потоки ввода-вывода местами.
1. Что выведет команда `/proc/$$/environ`? Как еще можно получить аналогичный по содержанию вывод?
1. Используя `man`, опишите что доступно по адресам `/proc/<PID>/cmdline`, `/proc/<PID>/exec`.
1. Узнайте, какую наиболее старшую версию набора инструкций SSE поддерживает ваш процессор с помощью `/proc/cpuinfo`.

# Ответы

1. Если я правильно понял вопрос (не уверен), то cd - это builtin. Мне так кажется по 2-м причинам. 1 - `type -a cd` даёт такой ответ, 2 - это команда, имеющая непосредственное отношение к shell'у, и по-моему достаточно логично, чтобы она была в него встроена.

**дополнение - cd не является программой, потому как при запуске она должна была бы менять cwd родительского процесса, а это не очень корректно и безопасно.**

2. `grep -c <come_string> <some_file>`

3. Судя по выводу `pstree -p` или `lsof -p 1` - это systemd. С системой ubuntu? Немного знаком - работал в Ubuntu, Debian, Centos - по мере необходимости, но на таком, лайтовом уровне.. ;)

**дополнение - знаю что есть ещё init.d (если об этом речь) - сталкивался в разных дистрибутивах, но вообще не парился, думал что это одно и то же ;)**

4. `ls no_such_file 2>/dev/pts/1`

1. `(cat < no_such_file) 2> test2` => `cat test2` => `-bash: no_such_file: No such file or directory`

1. Думаю, что да. Нужно будет перенаправить в /dev/tty1 (например) и тогда в консоли, нажав ctrl-alt-f1. Хотя явно в вопросе должен быть подвох.. ;)

1. Я не уверен, но кажется так... Этой командой я запускаю интерпретатор bash с файловым дескриптором 5 и перенаправляю его вывод в stdout. Хотя мне не очень понятно - если я запускаю `bash 5>/dev/null` - **вроде бы** результат тот же. Тут я честно скажу - похоже не до конца разобрался..
Если я выполню `echo netology > /proc/$$/fd/5` до `bash 5>&1`, то выйдет ошибка и даже понятно почему - нет открытого файла с дескриптором 5. Если же запустить после - слово `netology` появляется на экране, т.к. мы отправили это слово напрямую в файл-дескриптор 5 текущего процесса.
Перепроверил - если запустить так - `bash 5>/dev/null` - то вывода `netology` не будет.
Наверно тогда отвечу на этот вопрос так: командой `bash 5>&1` я запускаю bash и одновременно даю указание перенаправлять всё, что попадёт в fd 5 - в stdout. И следующей командой именно направляю слово `netology` в тот самый fd 5, и как следствие - в stdout.

8. У меня не получается.. :) Просто перенаправить stderr в stdout - легко, работает. Поменять местами, даже через промежуточный запуск bash аналогично предыдущему - у меня не получилось..

**Дополнение. Пишу сам, без подсказки :) Если вместо > представлять =, а вместо 1,2 и 5 - переменные - всё становится очевидно**

`ls no_such_file 5>&1 1>&2 2>&5 | grep no`

8. Наверно всё-таки команда `cat /proc/$$/environ`? Выведет изначальные переменные среды окружения текущего процесса. 

8. `/proc/<PID>/cmdline` - содержит полную строку запуска, включая ключи запуска. `/proc/<PID>/exe` - ссылка на исполняемый файл.

8. Во флагах указан максимальный `sse4_2`
